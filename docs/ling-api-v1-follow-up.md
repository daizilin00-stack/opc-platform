Ling API 团队：

感谢提供V1.0技术对接文档，我方已审阅完毕。整体接口设计清晰规范，三层凭证体系（租户令牌 / 用户令牌 / 环境密钥）与账单明细查询功能符合OPC平台的多租户架构需求，准备按文档第8章联调流程推进对接。

在正式启动联调前，请协助确认以下关键事项，以便我们一次性配齐开发环境：

**一、模型支持清单**

除文档示例中已明确的 `gpt-4o` / `gpt-4o-mini` 外，OPC平台业务场景还需要以下模型支持，请确认是否已接入并提供对应的模型ID与计费倍率：

| 模型 | 用途 | 是否支持 | 模型ID | 计费倍率 |
|------|------|----------|--------|----------|
| Claude 3.5 Sonnet / Claude 3 Opus | 合规海外模型通道（复杂推理/创意任务） | 待确认 | | |
| DeepSeek-V3 / DeepSeek-R1 | 国内模型替代方案（成本敏感型任务） | 待确认 | | |
| 文本 Embedding 模型（如 `text-embedding-3-large`） | 硅基员工平台向量检索 | 待确认 | | |
| DALL-E 3 / 其他图片生成模型 | 内容共创平台（图片生产） | 待确认 | | |
| Whisper / 其他音频转写模型 | 内容生产（音视频处理） | 待确认 | | |

如部分模型尚未接入，请告知预计排期，我们评估是否需要并行对接其他渠道。

**二、功能特性支持**

请确认以下OpenAI兼容特性是否全部支持：

- ✅ 流式SSE（文档已明确支持）
- ⚠️ Function Calling / Tools 调用：gpt-5.4、claude-sonnet-5、kimi-k2.5 支持；deepseek-v4-flash 未支持（详见 `ling-api-v1-test-report.md`）
- ⚠️ JSON Mode / Structured Output（结构化输出）：gpt-5.4、deepseek-v4-flash、kimi-k2.5 支持；claude-sonnet-5 未支持（详见 `ling-api-v1-test-report.md`）
- ❓ 图片生成（DALL-E系列）
- ❓ 音频转写（Whisper）

以上功能对硅基员工平台的自动化任务执行至关重要，需要提前确认兼容性。

**三、并发与限流策略**

请提供正式的生产环境限流阈值，用于我们评估平台可售卖的Token额度上限：

- 每分钟请求数（RPM）上限
- 每分钟Token数（TPM）上限
- 并发连接数上限
- 触发限流（429）后的重试建议策略（如指数退避参数）

**四、商务折扣与结算**

- OPC平台适用的折扣率是否按文档所述执行？
- 结算周期（月度/预付费）及最低消费要求
- 是否有阶梯折扣政策（月调用量达到一定规模后费率进一步下调）

**五、安全与合规**

- **IP白名单**：生产环境部署后我方将提供出口IP段，请确认是否支持CIDR格式配置，以及白名单变更的生效时效。
- **密钥轮换**：文档已支持自动化创建与轮换，请确认是否有强制轮换周期要求，或建议的最佳实践。
- **数据合规**：请求数据是否经新加坡节点转发？是否有数据留存/审计日志保留政策？（涉及中新跨境数据专线合规要求）

**六、联调环境**

请提供以下信息以便启动联调：

- 联调环境Base URL（区别于生产环境 `https://lingapi.com`）
- 联调账号（租户名/密码）
- 联调环境可用模型清单（可能仅开放部分模型）
- 测试额度上限（如有限制）

---

---

## 实测结果更新（2026-07-17）

专项测试脚本 `opc-platform/scripts/test-ling-api-advanced.py` 已执行完毕，完整报告见：

**`opc-platform/docs/ling-api-v1-test-report.md`**

主要结论：
- **Function Calling**：gpt-5.4、claude-sonnet-5、kimi-k2.5 支持；deepseek-v4-flash 未支持。
- **JSON Mode**：gpt-5.4、deepseek-v4-flash、kimi-k2.5 支持；claude-sonnet-5 未支持。
- **品牌隔离**：响应头仍暴露 `X-New-Api-Version` / `X-Oneapi-Request-Id`，错误信息含 `new_api_error`，需 LingAPI 网关做脱敏或 OPC 反向代理重写。
- **Usage 精度**：gpt-5.4 流式 `usage` 为 `null`，且非流式 `prompt_tokens` 异常偏高，需进一步确认。
- **并发**：10 并发 30 请求成功率 100%，平均延迟 2.2s，未触发限流。

后续行动：待 LingAPI 确认以上问题并给出修复/解释后，推进 NewAPI 上游渠道配置。

*更新人：团坐009*  
*日期：2026-07-17*

